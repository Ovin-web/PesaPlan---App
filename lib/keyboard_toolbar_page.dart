import 'package:flutter/material.dart';

class KeyboardToolbarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Keyboard Toolbar'),
      ),
      body: Center(
        child: Text('Keyboard Toolbar Page'),
      ),
    );
  }
}
